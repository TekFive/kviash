rootProject.name = "kviash"

// Standalone build: sibling libs (ack, jfk) are resolved from JitPack (see build.gradle.kts).
// Full-source development happens in the AIDEWAY umbrella build, which includes all the libs
// together.
